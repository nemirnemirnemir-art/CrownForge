# Decisions

- Session state docs are stored at repo root as `progress.md`, `tasks.md`, `decisions.md`, and `handoff.md`.
- Status tracking uses concise English bullet points instead of narrative notes.
- Morale breakdown, vzor pause/hover, `TownMenu` on `V`, debug menu sections, building search, and encounter-flow `TickManager` safety are treated as complete.
- Worker D is treated as mostly complete unless new gaps are found.
- Encounter-flow and audit-runner tests are treated as current verification baseline.
- Remaining priorities are Worker A/B/C campaign completion, possible leak warnings, and documentation cleanup.
