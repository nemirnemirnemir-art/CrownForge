# FRAGILE_CODE

| Путь | Паттерн | Серьёзность | Детали | Рекомендация |
|------|---------|-------------|--------|--------------|

| `res://scenes/heroes/HeroOnField.tscn` | Missing script dependency | 🔴 Critical | В `.tscn` есть `ext_resource` на `res://scripts/hero/states/HeroMovingState.gd` и `res://scripts/hero/states/HeroSimpleAttackState.gd`, но этих файлов нет (не находятся в `scripts/hero_states`). Потенциальный load error при инстансе сцены. | Обновить сцену на актуальные state scripts (например `HeroMovingToCombatState`, `HeroAttackingState`), либо убрать/заменить узлы/ресурсы, которые требуют старые скрипты. |
| `res://scripts/mob/states/MobHealState.gd` | Absolute node path (`/root/...`) | 🔴 Critical | `BattleCore.get_node_or_null("/root/GameScene/Wall")` — зависит от имени текущей сцены и точного пути узла. | Использовать group (`wall`) или явную ссылку, переданную из `GameScene`/`BattleCore`. |
| `res://core/population_core.gd` | Absolute autoload lookup (`/root/ArtifactCore`) | 🟡 High | `get_node_or_null("/root/ArtifactCore")` — хрупко к имени autoload и моменту инициализации. | Инъекция зависимости (ArtifactCore как ссылка), либо `ArtifactCore` emits signal о бонусах. |
| `res://core/save_core.gd` | Autoload enumeration + `/root/<autoload>` lookup | 🟡 High | Перебирает autoload’ы и делает `get_node_or_null("/root/%s" % autoload_name)` для save/load. | Явный реестр save-модулей; статический список ключей, тест на отсутствие. |
| `res://core/save_core.gd` | Direct mutation of other singleton internals | 🟡 High | В reset прогресса мутирует приватные поля (например `EconomyCore._current_gold`, `StageCore._current_stage`) и вручную эмитит `EventBus.*`. | Вынести “reset” в публичные методы соответствующих core (без доступа к приватным полям), единый контракт reset. |
| `res://scripts/ui/*` + `res://scripts/ui/town/*` | Implicit dependency via groups | 🟡 High | Частые `get_tree().get_first_node_in_group("main_ui")` для добавления tooltip/popup (а также `hero_bar`, `hero_card`, `spell_panel`). Если группы/ноды не в дереве — поведение меняется. | Централизовать overlay layer (одна точка для popup’ов), документировать группы и гарантировать их наличие в главной сцене. |
| `res://scripts/game/GameScene.gd` | Runtime fallback node creation | 🟡 High | Создаёт `HeroPivot` (Node2D), `WaveTimerBar` (instantiate), ghost-building `Sprite2D` при отсутствии в дереве. Это прячет ожидания структуры сцены и может приводить к “двойным” узлам. | Зафиксировать узлы в `GameScene.tscn` и оставить только assert/log при отсутствии. |

| `res://core/battle_core.gd` | Implicit dependency via group `game_scene` | 🟡 High | `start_wave()` и завершение волны выполняются только если `current_scene.is_in_group("game_scene")`. Если сцена забыта/переименована/не добавлена в группу — волны не стартуют/не завершаются. | Зафиксировать контракт “active gameplay scene” (один источник истины), либо инъекция ссылки на `GameScene`, либо отдельный `GameModeCore`. |
| `res://scenes/map/Wall.tscn` + consumers | Implicit dependency via group `wall` | 🟡 High | Стена попадает в группу `wall` через поле сцены: `[node ... groups=["wall"]]`. Код в моб-стейтах и UI использует `get_first_node_in_group("wall")`. Если инстансится другая сцена/Wall удалён/группа потеряна — логика навигации/атаки ломается. | Документировать “обязательные группы” и валидировать их наличие при старте `GameScene` (assert/log). |
| `EventBus.wave_failed(wave_number)` | Missing emitter (по текущему проходу) | 🟡 High | Listener подтверждён (`HeroCardSignals.gd`), но `EventBus.wave_failed.emit(...)` не найден по проекту в этом проходе. Риск: UI/логика ждёт событие, которого нет. | Либо добавить emitter в таймаут-логику волн, либо убрать/задокументировать недоступный сигнал. |

| `res://scripts/map/MapMarker.gd` + `res://scripts/map/MapMarkerService.gd` | Implicit dependency via marker groups | 🟡 High | `MapMarker` сам добавляет себя в группы (`spawn_markers`, `defense_markers`, `portal_markers`, `bridge_markers`, `wall_markers`...) по `marker_type`. `MapMarkerService` полагается на эти группы и при отсутствии marker’ов возвращает fallback координаты (и `push_warning`). | В `GameScene` валидировать наличие ключевых marker’ов (portal/bridge/wall) и явно логировать/останавливать игру в dev build при отсутствии. |

